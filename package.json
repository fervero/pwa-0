{
  "name": "hello-pwa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:front": "cd hello-front && npm install",
    "install:back": "cd hello-back && npm install",
    "install": "npm run install:front && npm run install:back",
    "build": "cd hello-front && npm run build",
    "install-n-build": "npm run install && npm run build",
    "serve": "cd hello-back && npm run start",
    "start": "npm run install && npm run build && npm run serve"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "18.16.0",
    "npm": "9.6.7"
  }
}
